defaults:
  - llm: phi-4-mini-instruct
  - _self_

exp_name: debug
run_dir: ./logs/${exp_name}/${now:%Y-%m-%d_%H-%M-%S_%f}

hydra:
  run:
    dir: ${run_dir}

prompt_generator:
  max_length: 2048

seed: 42
task: ???

retriever: contriever
num_candidates: 20  # Number of candidate profiles to rerank
num_rerank: 5
